{% extends 'base.html.twig' %}

{% block title %}New Song{% endblock %}

{% block body %}
    <h1>Create new Song</h1>

    <div class="container" style="background: #aacd4e; border: solid #222222">
        <div class="row">
            <div class="col-sm-3"></div>
            <div class="col-sm-6">
                {{ include('song/_form.html.twig') }}

                <a href="{{ path('song_index') }}" class="btn btn-info">back to list</a>
            </div>
            <div class="col-sm-3"></div>
        </div>
    </div>


{% endblock %}
